Simple Todo App

Open `index.html` in a browser or run a static server and visit http://localhost:8000

Quick server (Python 3):

```bash
python3 -m http.server 8000
```

Usage:
- Type a todo into the input and press `Add` or Enter.
- Each todo appears in the list with a `Delete` button.
